#include <stdio.h>
/**
* main - the function
* Description: it uses printf to output a string
* @void: no parameter
* Return: always 0
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return(0);
}
