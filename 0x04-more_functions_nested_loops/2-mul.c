#include "main.h"
/**
 *mul - the function
 *Description: It multiplies two integers and returns result
 *@a: a parameter
 *@b: other parameter
 *Return: returns result(success)
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
