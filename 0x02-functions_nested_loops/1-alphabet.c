#include "main.h"
/**
* print_alphabet - function
* Description: prints alphabet in lower case
* @void: no parameter
* Return: always 0
*/

void print_alphabet(void)
{
	int a = 'a';
	int b = 'z';
	int i = '\n';
	int c = 'a';

	while (c >= 'a' && c <= 'b')
	{
		_putchar(c);
	}
	_putchar(i);
	return (0);
}
