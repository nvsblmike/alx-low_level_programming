#include "main.h"
/**
*_islower - It outputs the one that is lower
*@c: this isnt it
*Description: This program outputs the one thays lower
*Return: Always 0(Success)
*/
int _islower(int c)
{
if (c >= 99 && c != 67)
{
return (1);
}
else
{
return (0);
}
}
